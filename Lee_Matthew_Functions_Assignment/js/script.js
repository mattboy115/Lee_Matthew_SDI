//Lee_Matthew_Functions_Assignment_5/26/2015

//Function for Dimensions of a Sphere

var 

//Function for Dimensions of a Rectangular Prism

alert

var length = prompt("What's the length of this prism?");
var width = prompt("What's the width?");
var height = prompt("What is the height?");

var calcVolume(length, width, height);

function calcVolume(l, w, h) {
	area = l * w * h;
	console.log(area);
}

calcVolume;